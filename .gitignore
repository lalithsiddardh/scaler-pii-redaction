# The audit map pairs every original value with its pseudonym and therefore
# re-identifies the document.  It must never be committed or published.
redaction_map.json
*redaction_map*.json

# Gold set and anything else generated by a local run.
gold_entities.json
redaction_summary.json
out.docx
*.docx.tmp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Local tooling
.DS_Store
Thumbs.db
.vscode/
.idea/
